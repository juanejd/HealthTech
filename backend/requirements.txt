fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0,<3.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
python-dotenv>=1.0.0
# Hardware drivers — required only on Raspberry Pi; guarded by try/except import
# in servo_controller.py and sensor_manager.py, so the test suite runs without them.
gpiozero>=2.0
pigpio>=1.78
